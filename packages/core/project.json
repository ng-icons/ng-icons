{
  "name": "core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/core/src",
  "prefix": "ng",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/core"],
      "defaultConfiguration": "production",
      "options": {
        "commands": ["nx run core:build-lib", "nx run core:build-schematics"],
        "parallel": false
      },
      "configurations": {
        "production": {},
        "development": {}
      }
    },
    "build-lib": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/packages/core"],
      "defaultConfiguration": "production",
      "options": {
        "project": "packages/core/ng-package.json",
        "tsConfig": "packages/core/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "packages/core/tsconfig.lib.prod.json"
        },
        "development": {}
      }
    },
    "build-schematics": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/core/schematics"],
      "options": {
        "commands": [
          "npx tsc -p packages/core/schematics/tsconfig.json",
          "cp packages/core/schematics/**/*.json dist/packages/core/schematics/"
        ],
        "parallel": false
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "nx-release-publish": {
      "dependsOn": ["build"],
      "options": {
        "packageRoot": "{workspaceRoot}/dist/{projectRoot}"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "{projectRoot}/../../coverage/packages/core"
      }
    }
  }
}
